package com.zaaam.nettra.browserui

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

/**
 * Feature-browser-ui — Compose UI for Nettra.
 * Mockup in /mockup/index.html is the source of truth before this module is fleshed out.
 * FR-1 tab strip, FR-2 address bar, FR-6 Fire Button, FR-7 Privacy Report overlay.
 */
@Composable
fun BrowserUiPlaceholder() {
    Text("Nettra Browser UI — implement after mockup approval")
}
